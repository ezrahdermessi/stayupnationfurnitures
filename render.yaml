services:
  - type: web
    name: stayup-nation
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn stayup_furniture.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
